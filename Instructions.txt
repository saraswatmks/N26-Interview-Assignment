Instructions

1. Make sure conda is installed in your machine. Run the following commands from the root directory of the project. Make sure the data is accessible from this directory.
2. Create a conda environment using the following command: conda env create -f environment.yml
2. Run jupyter server using: jupyter notebook
3. Set file paths in the notebooks

Python version: python=3.7

